{
  "name": "wordpress-standards-workflow",
  "private": true,
  "type": "module",
  "description": "Kit-only dev tooling: runs the test suite for every theme/**/*.test.mjs, examples/**/*.test.mjs and tests/*.test.mjs file. Never imported by a project theme.",
  "scripts": {
    "test": "node --test $(git ls-files --cached --others --exclude-standard 'theme/**/*.test.mjs' 'examples/**/*.test.mjs' 'tests/*.test.mjs')"
  },
  "devDependencies": {
    "postcss": "^8.5.26",
    "postcss-selector-parser": "^6.1.4",
    "vite": "^6.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
